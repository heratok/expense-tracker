version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pet-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5000:5000"
